[toolchain]
channel = "stable"
components = ["rustfmt", "clippy"]
profile = "minimal"

# Note: CI/CD uses this file automatically via dtolnay/rust-toolchain@stable
# Using 'stable' to ensure compatibility with latest Solana/Anchor dependencies
