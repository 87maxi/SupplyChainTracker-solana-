/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findDeployerPda } from "../pdas";
import { SC_SOLANA_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_DEPLOYER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  132, 178, 139, 113, 231, 123, 71, 38,
]);

export function getCloseDeployerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_DEPLOYER_DISCRIMINATOR,
  );
}

export type CloseDeployerInstruction<
  TProgram extends string = typeof SC_SOLANA_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountDeployer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountDeployer extends string
        ? WritableAccount<TAccountDeployer>
        : TAccountDeployer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseDeployerInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseDeployerInstructionDataArgs = {};

export function getCloseDeployerInstructionDataEncoder(): FixedSizeEncoder<CloseDeployerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_DEPLOYER_DISCRIMINATOR }),
  );
}

export function getCloseDeployerInstructionDataDecoder(): FixedSizeDecoder<CloseDeployerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseDeployerInstructionDataCodec(): FixedSizeCodec<
  CloseDeployerInstructionDataArgs,
  CloseDeployerInstructionData
> {
  return combineCodec(
    getCloseDeployerInstructionDataEncoder(),
    getCloseDeployerInstructionDataDecoder(),
  );
}

export type CloseDeployerAsyncInput<
  TAccountConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountDeployer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config?: Address<TAccountConfig>;
  admin: TransactionSigner<TAccountAdmin>;
  /** Deployer PDA to close - returns lamports to admin */
  deployer?: Address<TAccountDeployer>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getCloseDeployerInstructionAsync<
  TAccountConfig extends string,
  TAccountAdmin extends string,
  TAccountDeployer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SC_SOLANA_PROGRAM_ADDRESS,
>(
  input: CloseDeployerAsyncInput<
    TAccountConfig,
    TAccountAdmin,
    TAccountDeployer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseDeployerInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountAdmin,
    TAccountDeployer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SC_SOLANA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: true },
    deployer: { value: input.deployer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.deployer.value) {
    accounts.deployer.value = await findDeployerPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("deployer", accounts.deployer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCloseDeployerInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseDeployerInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountAdmin,
    TAccountDeployer,
    TAccountSystemProgram
  >);
}

export type CloseDeployerInput<
  TAccountConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountDeployer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  admin: TransactionSigner<TAccountAdmin>;
  /** Deployer PDA to close - returns lamports to admin */
  deployer: Address<TAccountDeployer>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCloseDeployerInstruction<
  TAccountConfig extends string,
  TAccountAdmin extends string,
  TAccountDeployer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SC_SOLANA_PROGRAM_ADDRESS,
>(
  input: CloseDeployerInput<
    TAccountConfig,
    TAccountAdmin,
    TAccountDeployer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseDeployerInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountAdmin,
  TAccountDeployer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SC_SOLANA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: true },
    deployer: { value: input.deployer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("deployer", accounts.deployer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCloseDeployerInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseDeployerInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountAdmin,
    TAccountDeployer,
    TAccountSystemProgram
  >);
}

export type ParsedCloseDeployerInstruction<
  TProgram extends string = typeof SC_SOLANA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    admin: TAccountMetas[1];
    /** Deployer PDA to close - returns lamports to admin */
    deployer: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CloseDeployerInstructionData;
};

export function parseCloseDeployerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseDeployerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      admin: getNextAccount(),
      deployer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCloseDeployerInstructionDataDecoder().decode(instruction.data),
  };
}
