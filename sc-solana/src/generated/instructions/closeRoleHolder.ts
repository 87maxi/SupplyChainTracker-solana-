/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAdminPda, findConfigPda } from "../pdas";
import { SC_SOLANA_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_ROLE_HOLDER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([31, 181, 61, 92, 127, 199, 245, 153]);

export function getCloseRoleHolderDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_ROLE_HOLDER_DISCRIMINATOR,
  );
}

export type CloseRoleHolderInstruction<
  TProgram extends string = typeof SC_SOLANA_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountRoleHolder extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountAdmin extends string
        ? ReadonlyAccount<TAccountAdmin>
        : TAccountAdmin,
      TAccountRoleHolder extends string
        ? WritableAccount<TAccountRoleHolder>
        : TAccountRoleHolder,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseRoleHolderInstructionData = {
  discriminator: ReadonlyUint8Array;
  role: string;
};

export type CloseRoleHolderInstructionDataArgs = { role: string };

export function getCloseRoleHolderInstructionDataEncoder(): Encoder<CloseRoleHolderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["role", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: CLOSE_ROLE_HOLDER_DISCRIMINATOR }),
  );
}

export function getCloseRoleHolderInstructionDataDecoder(): Decoder<CloseRoleHolderInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["role", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getCloseRoleHolderInstructionDataCodec(): Codec<
  CloseRoleHolderInstructionDataArgs,
  CloseRoleHolderInstructionData
> {
  return combineCodec(
    getCloseRoleHolderInstructionDataEncoder(),
    getCloseRoleHolderInstructionDataDecoder(),
  );
}

export type CloseRoleHolderAsyncInput<
  TAccountConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountRoleHolder extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config?: Address<TAccountConfig>;
  admin?: Address<TAccountAdmin>;
  /**
   * RoleHolder account to close - returns lamports to admin
   * Seeds derived from the stored account field (same pattern as holder_remove)
   */
  roleHolder: Address<TAccountRoleHolder>;
  systemProgram?: Address<TAccountSystemProgram>;
  role: CloseRoleHolderInstructionDataArgs["role"];
};

export async function getCloseRoleHolderInstructionAsync<
  TAccountConfig extends string,
  TAccountAdmin extends string,
  TAccountRoleHolder extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SC_SOLANA_PROGRAM_ADDRESS,
>(
  input: CloseRoleHolderAsyncInput<
    TAccountConfig,
    TAccountAdmin,
    TAccountRoleHolder,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseRoleHolderInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountAdmin,
    TAccountRoleHolder,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SC_SOLANA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
    roleHolder: { value: input.roleHolder ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.admin.value) {
    accounts.admin.value = await findAdminPda({
      config: getAddressFromResolvedInstructionAccount(
        "config",
        accounts.config.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("roleHolder", accounts.roleHolder),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCloseRoleHolderInstructionDataEncoder().encode(
      args as CloseRoleHolderInstructionDataArgs,
    ),
    programAddress,
  } as CloseRoleHolderInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountAdmin,
    TAccountRoleHolder,
    TAccountSystemProgram
  >);
}

export type CloseRoleHolderInput<
  TAccountConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountRoleHolder extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  admin: Address<TAccountAdmin>;
  /**
   * RoleHolder account to close - returns lamports to admin
   * Seeds derived from the stored account field (same pattern as holder_remove)
   */
  roleHolder: Address<TAccountRoleHolder>;
  systemProgram?: Address<TAccountSystemProgram>;
  role: CloseRoleHolderInstructionDataArgs["role"];
};

export function getCloseRoleHolderInstruction<
  TAccountConfig extends string,
  TAccountAdmin extends string,
  TAccountRoleHolder extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SC_SOLANA_PROGRAM_ADDRESS,
>(
  input: CloseRoleHolderInput<
    TAccountConfig,
    TAccountAdmin,
    TAccountRoleHolder,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseRoleHolderInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountAdmin,
  TAccountRoleHolder,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SC_SOLANA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
    roleHolder: { value: input.roleHolder ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("roleHolder", accounts.roleHolder),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCloseRoleHolderInstructionDataEncoder().encode(
      args as CloseRoleHolderInstructionDataArgs,
    ),
    programAddress,
  } as CloseRoleHolderInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountAdmin,
    TAccountRoleHolder,
    TAccountSystemProgram
  >);
}

export type ParsedCloseRoleHolderInstruction<
  TProgram extends string = typeof SC_SOLANA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    admin: TAccountMetas[1];
    /**
     * RoleHolder account to close - returns lamports to admin
     * Seeds derived from the stored account field (same pattern as holder_remove)
     */
    roleHolder: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CloseRoleHolderInstructionData;
};

export function parseCloseRoleHolderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseRoleHolderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      admin: getNextAccount(),
      roleHolder: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCloseRoleHolderInstructionDataDecoder().decode(instruction.data),
  };
}
