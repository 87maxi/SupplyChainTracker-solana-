/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { SC_SOLANA_PROGRAM_ADDRESS } from "../programs";

export const QUERY_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  64, 154, 181, 109, 94, 172, 201, 243,
]);

export function getQueryConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    QUERY_CONFIG_DISCRIMINATOR,
  );
}

export type QueryConfigInstruction<
  TProgram extends string = typeof SC_SOLANA_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type QueryConfigInstructionData = { discriminator: ReadonlyUint8Array };

export type QueryConfigInstructionDataArgs = {};

export function getQueryConfigInstructionDataEncoder(): FixedSizeEncoder<QueryConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: QUERY_CONFIG_DISCRIMINATOR }),
  );
}

export function getQueryConfigInstructionDataDecoder(): FixedSizeDecoder<QueryConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getQueryConfigInstructionDataCodec(): FixedSizeCodec<
  QueryConfigInstructionDataArgs,
  QueryConfigInstructionData
> {
  return combineCodec(
    getQueryConfigInstructionDataEncoder(),
    getQueryConfigInstructionDataDecoder(),
  );
}

export type QueryConfigInput<TAccountConfig extends string = string> = {
  config: Address<TAccountConfig>;
};

export function getQueryConfigInstruction<
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof SC_SOLANA_PROGRAM_ADDRESS,
>(
  input: QueryConfigInput<TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): QueryConfigInstruction<TProgramAddress, TAccountConfig> {
  // Program address.
  const programAddress = config?.programAddress ?? SC_SOLANA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("config", accounts.config)],
    data: getQueryConfigInstructionDataEncoder().encode({}),
    programAddress,
  } as QueryConfigInstruction<TProgramAddress, TAccountConfig>);
}

export type ParsedQueryConfigInstruction<
  TProgram extends string = typeof SC_SOLANA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
  };
  data: QueryConfigInstructionData;
};

export function parseQueryConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedQueryConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { config: getNextAccount() },
    data: getQueryConfigInstructionDataDecoder().decode(instruction.data),
  };
}
