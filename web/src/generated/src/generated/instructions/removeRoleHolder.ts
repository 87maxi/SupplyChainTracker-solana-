/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAdminPda } from "../pdas";
import { SC_SOLANA_PROGRAM_ADDRESS } from "../programs";

export const REMOVE_ROLE_HOLDER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([25, 78, 125, 133, 147, 20, 6, 176]);

export function getRemoveRoleHolderDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REMOVE_ROLE_HOLDER_DISCRIMINATOR,
  );
}

export type RemoveRoleHolderInstruction<
  TProgram extends string = typeof SC_SOLANA_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountRoleHolder extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountAdmin extends string
        ? ReadonlyAccount<TAccountAdmin>
        : TAccountAdmin,
      TAccountRoleHolder extends string
        ? WritableAccount<TAccountRoleHolder>
        : TAccountRoleHolder,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveRoleHolderInstructionData = {
  discriminator: ReadonlyUint8Array;
  role: string;
};

export type RemoveRoleHolderInstructionDataArgs = { role: string };

export function getRemoveRoleHolderInstructionDataEncoder(): Encoder<RemoveRoleHolderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["role", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: REMOVE_ROLE_HOLDER_DISCRIMINATOR }),
  );
}

export function getRemoveRoleHolderInstructionDataDecoder(): Decoder<RemoveRoleHolderInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["role", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getRemoveRoleHolderInstructionDataCodec(): Codec<
  RemoveRoleHolderInstructionDataArgs,
  RemoveRoleHolderInstructionData
> {
  return combineCodec(
    getRemoveRoleHolderInstructionDataEncoder(),
    getRemoveRoleHolderInstructionDataDecoder(),
  );
}

export type RemoveRoleHolderAsyncInput<
  TAccountConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountRoleHolder extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  admin?: Address<TAccountAdmin>;
  roleHolder: Address<TAccountRoleHolder>;
  systemProgram?: Address<TAccountSystemProgram>;
  role: RemoveRoleHolderInstructionDataArgs["role"];
};

export async function getRemoveRoleHolderInstructionAsync<
  TAccountConfig extends string,
  TAccountAdmin extends string,
  TAccountRoleHolder extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SC_SOLANA_PROGRAM_ADDRESS,
>(
  input: RemoveRoleHolderAsyncInput<
    TAccountConfig,
    TAccountAdmin,
    TAccountRoleHolder,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RemoveRoleHolderInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountAdmin,
    TAccountRoleHolder,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SC_SOLANA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
    roleHolder: { value: input.roleHolder ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.admin.value) {
    accounts.admin.value = await findAdminPda({
      config: getAddressFromResolvedInstructionAccount(
        "config",
        accounts.config.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("roleHolder", accounts.roleHolder),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRemoveRoleHolderInstructionDataEncoder().encode(
      args as RemoveRoleHolderInstructionDataArgs,
    ),
    programAddress,
  } as RemoveRoleHolderInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountAdmin,
    TAccountRoleHolder,
    TAccountSystemProgram
  >);
}

export type RemoveRoleHolderInput<
  TAccountConfig extends string = string,
  TAccountAdmin extends string = string,
  TAccountRoleHolder extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  admin: Address<TAccountAdmin>;
  roleHolder: Address<TAccountRoleHolder>;
  systemProgram?: Address<TAccountSystemProgram>;
  role: RemoveRoleHolderInstructionDataArgs["role"];
};

export function getRemoveRoleHolderInstruction<
  TAccountConfig extends string,
  TAccountAdmin extends string,
  TAccountRoleHolder extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SC_SOLANA_PROGRAM_ADDRESS,
>(
  input: RemoveRoleHolderInput<
    TAccountConfig,
    TAccountAdmin,
    TAccountRoleHolder,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RemoveRoleHolderInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountAdmin,
  TAccountRoleHolder,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SC_SOLANA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
    roleHolder: { value: input.roleHolder ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("roleHolder", accounts.roleHolder),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRemoveRoleHolderInstructionDataEncoder().encode(
      args as RemoveRoleHolderInstructionDataArgs,
    ),
    programAddress,
  } as RemoveRoleHolderInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountAdmin,
    TAccountRoleHolder,
    TAccountSystemProgram
  >);
}

export type ParsedRemoveRoleHolderInstruction<
  TProgram extends string = typeof SC_SOLANA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    admin: TAccountMetas[1];
    roleHolder: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RemoveRoleHolderInstructionData;
};

export function parseRemoveRoleHolderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveRoleHolderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      admin: getNextAccount(),
      roleHolder: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRemoveRoleHolderInstructionDataDecoder().decode(instruction.data),
  };
}
